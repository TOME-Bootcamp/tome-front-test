const formatConfig = {
  plugins: ['prettier-plugin-tailwindcss'],
  trailingComma: 'all',
  printWidth: 100,
  semi: true,
  singleQuote: true,
  arrowParens: 'always',
};

export default formatConfig;
